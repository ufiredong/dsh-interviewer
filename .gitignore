# 依赖与构建产物
node_modules/
*.log
npm-debug.log*
pnpm-debug.log*
.npm/
.pnpm-store/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# 本地临时验证产物（不该进版本库）
.smoke/
*.tgz
.market-*.json

# 提交信息临时文件。教训：曾经用 .git-msg.tmp 传 commit message，
# 结果它在 `git add -A` 时被一起提交了进去。
.git-msg.tmp
.git-commit-msg.txt
*.tmp
