# 更新日志

遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.0] —— 首个公开发布版

从"本机能跑"变成"别人能用"的那一版。之前的版本只在作者本机验证过，
依赖了本机恰好存在的东西，装到别人机器上不会报错、但会静默降级。

### 修复

- **插件不再依赖用户磁盘上的技能目录（发布阻塞项）。**
  面试 SOP 原本只住在 `~/.dsh/skills/interviewer/`，`lib/prompt.js` 按绝对路径读它。
  别人装了插件没有这个目录，会静默降级到压缩版 `FALLBACK_SOP` —— 面试官质量掉一档
  且不报任何错，装的人只会觉得"不好用"。现在 SOP 随包分发在 `skills/interviewer/`，
  默认读包内那份（`lib/skill.js` 是全插件唯一定义 SOP 位置的地方）。
- **技能与面板共用一份 SOP。** 新增 `lib/skill.js`，通过 `ctx.skills.register()`
  把 SOP 注册成运行时技能，`resourceBase` 指向包内目录。装了插件就等于装了技能，
  主会话里也能 `skill interviewer`。注册失败只告警，不会弄崩宿主启动。

### 新增

- **单轮失败自动重试。** 实测确认推理（thinking）token 也计入 `maxTokens`，而模型
  思考长度波动极大 —— 同一段对话同一额度，有时 3 秒答完，有时思考 14 秒一个字没吐
  直接 `max-tokens`。现在如果一轮没有产出任何内容，会自动把额度翻倍重试一次，
  并追加一句"别想了直接说"。已经写出半句话的情况不重试（那会得到不连贯的答案）。
- **面试状态机（`lib/state.js`）。** 无状态设计下，模型每轮都要从零推导"该问什么"，
  而上下文里最抢眼的是"他刚才没回答我"，于是 2026-09-20 那场模拟面试在同一个话题上
  追了 5 轮（SOP 只允许 2 轮），代价是放掉了别处一段教科书级的含糊回答。
  现在状态显式化：面试计划、当前话题、追了几次、剩余时间、记下的边界。

  两个设计细节是实测逼出来的，不是拍脑袋定的：

  - **标记只用一行纯文本，不用 JSON。** 每轮让模型吐 `<!--topic: 分库分表-->`
    而不是一段带 `topic`/`done`/`boundary`/`note`/`plan` 五个字段的 JSON。
    字段越少越容易被照做；计划只在还没有计划时索要一次。
  - **状态机的正确性不依赖模型配合。** 实测标记命中率只有 50% 上下，
    换措辞、把协议挪到提示词最末尾都救不回来（挪末尾反而从 4/5 掉到 1/5）。
    所以到上限时提示词会**点名**下一个话题（"换到「数据库与扩展」"），
    而下一轮若仍没有标记，状态机就**乐观前进**，自己把话题指针推到那里。
    计划要不到时则装一份按方向预设的骨架。结果是即使模型完全不吐标记，
    广度推进依然成立。

  回归实测（`interview-sim/state-run.mjs`，11 轮真实模型调用，与无状态版同一套台词）：

  | | 无状态版 | 状态机版 |
  |---|---|---|
  | 单话题最多追问 | 4 轮（「多出来的 1.5 秒」反复重问） | **2 轮** |
  | 走过的不同话题 | 2 个 | **6 个** |
  | 收尾表现 | `max-tokens` 挂掉 | 「好，我整理一下」 |

- **每轮结束的标记格式说明**随提示词下发（`stateProtocolBlock`），
  并且协议文本会随状态变化 —— 拿到计划之后就不再索要 `plan` 行。
- **面板状态条。** 显示已用时间、计划进度、当前话题第几次提问，到上限时标红。
  它同时是一个可验证的读数 —— 追太多一眼就能看出来。
- **启动日志打印实际生效的 SOP 目录**，以及技能注册结果。装完不好用时能直接排查。

### 变更

- **README 重写为面向用户的公开文档**，新增「它能做什么」与「和其他插件的区别」两节。
  后者基于对市场数据（7795 条插件）的实际扫描：做「求职」的插件目前只有
  `recruiting-copilot`（招聘方视角）、`resume-reviser` / `dsh-resume-expert`
  （改简历）和 `dsh-scout`（面试前尽调）—— **没有一个是把面试当主业的**。
  所以那一节讲的是「产出是什么」的区别，不是功能多少。
- README 开头两段由引用块 + 列表改为**连续散文**：市场抓 README 前约 1200 字符
  做卡片简介时会把 markdown 压平成一行，列表符号和 `>` 会变成噪声。
  新增 `docs/readme-preview.mjs` 预览该效果，并断言第一个标题之前无列表/引用/表格符号。
- 顶部补上徽章（License / Node / DSH 版本要求）。刻意只用**静态**徽章 ——
  仓库还没推、npm 还没发，动态徽章（stars、npm version、CI）此时都会是坏的。
- 提示词装配从 `lib/index.js` 拆到 `lib/prompt.js`（纯 Node、不依赖 DSH），
  这样离线就能把提示词拼一遍做断言，而不是靠正则扫源码。
- `inject` 增加 `'skills'`。
- `engines.node` 从 `>=22` 放宽到 `>=20`，与 DSH 自身要求一致。
- 补齐发布元数据：`repository` / `bugs` / `homepage` / `author`、`LICENSE`、
  `files` 增加 `skills` 与 `LICENSE`、`exports` 增加 `"./skills/*"`。

### 已知限制

- 面板只注册 `shell.overlay` 一个槽位（右下角悬浮窗）。侧边栏入口注册成功但
  侧边栏不渲染，原因未定位，已放弃该入口。
- 面试评分的可视化面板还没做。评分目前通过 SOP 的 `scripts/score.mjs`
  （确定性加权计算）在会话里给出，不在插件 UI 里。

## [0.2.0]

- 面板与设置分离：点按钮先进设置弹窗（方向 / 职级 / 轮次 / 风格），确认后才进面试窗口。
- 面试官风格库（温和引导 / 务实工程 / 冷峻压测 / 学术严谨），定义见
  `references/05-voices.md`。
- 悬浮窗可拖拽、可收起，位置做视口钳制。
- 语音输入（浏览器内置 `SpeechRecognition`，中文），识别结果只填进草稿不自动发送。
- 系统提示词内联全部被引用的 `references/*.md`，并在最前面声明"没有文件系统访问权限"。
  此前模型会尝试调用工具去 `find` 那些被引用但未提供的文件，输出全是工具调用语法。

## [0.1.0]

- 初始版本：`shell.overlay` 悬浮面板，接 DSH 的 `llm` 服务真实对话。
